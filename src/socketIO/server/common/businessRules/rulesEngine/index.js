/**
 * TODO: build out logic if required
 * @returns 
 */
const rulesEngine = () => {
    const featureToggleWithUpdated = {
        // do stuff here
    }
    return featureToggleWithUpdated;
};

module.exports = rulesEngine;